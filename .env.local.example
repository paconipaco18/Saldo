# Anthropic — SERVER-ONLY. Never import this into a client component;
# all model calls must run inside app/api/** route handlers.
ANTHROPIC_API_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
